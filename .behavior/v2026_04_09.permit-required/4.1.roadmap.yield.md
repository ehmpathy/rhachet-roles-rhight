# roadmap: permit-required

## overview

two goals with different statuses:
- **goal 1**: permit.check.required — ✓ DONE
- **goal 2**: permit.search, permit.fetch — ✗ BLOCKED (see blocker.goal2.md)

---

## goal 1: permit.check.required (DONE)

static determination of permit requirements via local computation with IRC code citations.

### phases completed

| phase | name | status | verify |
|-------|------|--------|--------|
| 3 | domain objects | ✓ done | `rhx git.repo.test --what types` |
| 4 | transformers | ✓ done | `rhx git.repo.test --what unit` |
| 8 | permit.check.required skill | ✓ done | `rhx git.repo.test --what unit` |
| 9 | role registration | ✓ done | `npm run build` |

### verification

```
🐚 git.repo.test --what unit
   ├─ status: 🎉 passed
   ├─ stats: 10 passed, 0 failed, 0 skipped
```

---

## goal 2: permit.search + permit.fetch (BLOCKED)

search and fetch permits from Indianapolis-Marion County Accela portal.

### why blocked

Indianapolis uses **Accela** for permits. API access requires **agency registration** which is not public.

| approach | status |
|----------|--------|
| Accela API | blocked — requires Indianapolis DBNS partnership |
| webscrape | deferred — todo: use rhachet-roles-kermet |

### stubs created

| file | status |
|------|--------|
| `src/domain.roles/permiter/skills/permit.search/permit.search.sh` | stub (exit 2) |
| `src/domain.roles/permiter/skills/permit.fetch/permit.fetch.sh` | stub (exit 2) |
| `src/domain.operations/permit/searchPermits.ts` | throws blocked |
| `src/domain.operations/permit/fetchPermit.ts` | throws blocked |

### to unblock

1. implement rhachet-roles-kermet (browser automation role)
2. return to phases below

### blocked phases

| phase | name | blocked on |
|-------|------|------------|
| 0 | playwright install | kermet |
| 1 | scrape utilities | kermet |
| 2 | selectors + fixtures | kermet |
| 5 | domain operations (search/fetch) | kermet |
| 6 | permit.search skill | kermet |
| 7 | permit.fetch skill | kermet |

---

## summary

| goal | status | evidence |
|------|--------|----------|
| permit.check.required | ✓ DONE | unit tests pass (10/10) |
| permit.search | ✗ BLOCKED | todo: use rhachet-roles-kermet |
| permit.fetch | ✗ BLOCKED | todo: use rhachet-roles-kermet |

---

## reference

- blocker.goal2.md — full blocker documentation
- 5.3.verification.yield.md — test verification
