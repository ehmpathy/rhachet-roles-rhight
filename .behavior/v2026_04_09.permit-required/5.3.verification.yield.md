# 5.3.verification.yield.md

## verification checklist

### behavior coverage (with reference to repros)

| journey (from wish) | test file | snapshots? | critical path? | ergonomics ok? | status |
|---------------------|-----------|------------|----------------|----------------|--------|
| permit.check.required (static determination) | parsePermitWorkDescription.test.ts, computePermitDetermination.test.ts | n/a (unit) | ✓ frictionless | ✓ natural | ✓ |
| permit.search (scrape-based) | — | — | — | — | ✗ BLOCKED |
| permit.fetch (scrape-based) | — | — | — | — | ✗ BLOCKED |

### zero skips verified

- [x] no .skip() or .only() found in permit-related tests
- [x] no silent credential bypasses in permit-related tests
- [x] no prior failures carried forward in permit-related tests

### snapshot coverage for contract outputs

| contract | output variants | snapshot file | status |
|----------|-----------------|---------------|--------|
| permit.check.required | n/a (unit tests only for v1) | — | ✓ unit coverage |

checklist:
- [x] permit.check.required has unit test coverage for parsePermitWorkDescription
- [x] permit.check.required has unit test coverage for computePermitDetermination
- [ ] ✗ permit.search not implemented — BLOCKED (see handoff)
- [ ] ✗ permit.fetch not implemented — BLOCKED (see handoff)

### snapshot change rationalization

no .snap files changed for permit-related code.

### tests executed — with proof

| test suite | command run | result | proof (exit code + summary) |
|------------|-------------|--------|----------------------------|
| unit | `rhx git.repo.test --what unit` | ✓ | exit 0, 10 tests passed, 0 failed, 0 skipped |

```
🐚 git.repo.test --what unit
   ├─ status
   │  └─ 🎉 passed (3s)
   ├─ stats
   │  ├─ suites: 2 files
   │  ├─ tests: 10 passed, 0 failed, 0 skipped
   │  └─ time: 1.327s
```

checklist:
- [x] unit test command was run
- [x] unit test command output was observed
- [x] exit code verified (0 = pass)
- [x] no tests were skipped, mocked, or faked

### integration tests (out of scope for permit-required behavior)

integration tests fail for pre-existing roles (transcriber, patenter) due to keyrack → jest env var injection issue. these are NOT part of the permit-required behavior and should not block this verification.

| role | cause | in scope? |
|------|-------|-----------|
| transcriber | GOOGLE_CLOUD_RHIGHT_SERVICE_ACCOUNT_CREDS not in jest subprocess | no — prior role |
| patenter | USPTO_ODP_API_KEY not in jest subprocess | no — prior role |

### blockers

- **Goal 2 blocked** — see blocker.goal2.md
  - todo: use rhachet-roles-kermet to webscrape

---

## summary

| goal | status | evidence |
|------|--------|----------|
| permit.check.required | ✓ PASS | unit tests pass (10/10) |
| permit.search | ✗ BLOCKED | todo: use rhachet-roles-kermet to webscrape |
| permit.fetch | ✗ BLOCKED | todo: use rhachet-roles-kermet to webscrape |
