# blocker: 5.1.execution.phase0_to_phaseN

## what blocks

Goal 2 (permit.search + permit.fetch) requires browser automation to scrape the Indianapolis Accela permit portal.

The Accela API requires agency registration with Indianapolis DBNS, which is not available for public use.

## what was tried

1. Researched Accela API — requires partnership with Indianapolis DBNS (blocked)
2. Checked Open Indy Data Portal — permit records not exposed
3. Created stub implementations that exit with "blocked" status
4. Documented the blocker in `blocker.goal2.md`

Goal 1 (permit.check.required) was completed successfully — static determination works with IRC code citations.

## what is needed

**rhachet-roles-kermet** — a browser automation role that can:
- Launch headless browser (playwright)
- Navigate to Accela portal
- Fill search forms
- Extract permit data from results table
- Handle pagination and cache results

Once kermet exists, return to execute phases 0-7 for goal 2.
