# 3.2.distill.repros.experience._.v1

experience reproductions distillation for the patenter role.

---

## experience reproductions

| experience | entry point | user actions | expected outcome | test type |
|------------|-------------|--------------|------------------|-----------|
| prior art search | `rhx patent.priors.search` | provide query | list of relevant patents with relevance scores | integration |
| patent fetch | `rhx patent.priors.fetch` | provide exid | full patent document with claims | integration |
| route creation | `rhx patent.propose` | run command | route created, stones ready | integration |
| route with editor | `rhx patent.propose --open nvim` | run command | route created, editor opens | acceptance |
| empty search results | `rhx patent.priors.search` | vague query | empty results + suggestions | integration |
| invalid exid | `rhx patent.priors.fetch` | bad exid | clear error message | integration |

---

## journey test sketches

### journey.1 = prior art search

**entry point**: `rhx patent.priors.search --query "..."`

#### step table

| step | action | user sees |
|------|--------|-----------|
| t0 | before any changes | terminal ready |
| t1 | search with good query | list of patents with relevance scores |
| t2 | search with vague query | results + alert about specificity |
| t3 | search with no matches | empty list + refinement suggestions |

#### t1 success case (snapshot target)

```
$ rhx patent.priors.search --query "neural network model compression via distillation"

🦅 soar and see,

🔍 patent.priors.search
   ├─ query: neural network model compression via distillation
   ├─ results: 12
   └─ patents
      ├─ US20210234567A1 (0.92)
      │  └─ "Method for Knowledge Distillation in Neural Networks"
      ├─ US20200123456A1 (0.87)
      │  └─ "Compression Techniques for Deep Models"
      ├─ US20190098765A1 (0.81)
      │  └─ "Student-Teacher Network System"
      └─ ... (9 more)
```

#### snapshot coverage plan

- [x] t1 success search → `.snap`
- [x] t2 vague query alert → `.snap`
- [x] t3 no results + suggestions → `.snap`

---

### journey.2 = patent fetch

**entry point**: `rhx patent.priors.fetch --exid "..."`

#### step table

| step | action | user sees |
|------|--------|-----------|
| t0 | before any changes | terminal ready |
| t1 | fetch valid exid | full patent with claims, metadata |
| t2 | fetch invalid exid | error with format guidance |
| t3 | fetch nonexistent exid | error with verification hint |

#### t1 success case (snapshot target)

```
$ rhx patent.priors.fetch --exid US20210234567A1

🦅 got one,

📄 patent.priors.fetch
   ├─ exid: US20210234567A1
   ├─ title: Method for Knowledge Distillation in Neural Networks
   ├─ filed: 2021-01-15
   ├─ published: 2021-07-22
   ├─ inventors: Smith, John; Chen, Wei
   ├─ assignee: TechCorp Inc.
   │
   ├─ abstract
   │  ├─
   │  │
   │  │  A method for compression of deep neural network models through
   │  │  knowledge distillation from a teacher network to a smaller
   │  │  student network...
   │  │
   │  └─
   │
   ├─ claims (3)
   │  ├─ 1. A method for model compression, the method includes...
   │  ├─ 2. The method of claim 1, wherein the student network...
   │  └─ 3. A system for neural network distillation, the system includes...
   │
   └─ figures (2)
      ├─ FIG. 1: System architecture diagram
      └─ FIG. 2: Model train flow chart
```

#### snapshot coverage plan

- [x] t1 success fetch → `.snap`
- [x] t2 invalid format error → `.snap`
- [x] t3 not found error → `.snap`

---

### journey.3 = route creation

**entry point**: `rhx patent.propose`

#### step table

| step | action | user sees |
|------|--------|-----------|
| t0 | before any changes | no route extant |
| t1 | create route | route created with all stones |
| t2 | try create again | error: route already extant |

#### t1 success case (snapshot target)

```
$ rhx patent.propose

🦅 take to the sky,
   ├─ ✓ 0.idea.md
   ├─ ✓ 1.vision.stone
   ├─ ✓ 3.1.research.prior-art.favorable.stone
   ├─ ✓ 3.1.research.prior-art.adverse.stone
   ├─ ✓ 3.2.distill.claims.prior-art.stone
   ├─ ✓ 3.2.distill.claims.patentable.stone
   ├─ ✓ 3.2.distill.strategy.officeactions.stone
   ├─ ✓ 3.3.blueprint.patent.stone
   └─ ✓ 5.1.deliver.patent.latex.stone

🏔️ what peaks can we claim?
   ├─ .route/v2026_04_03.patent.propose/0.idea.md
   └─ ready for your invention

🌎 we'll track it down,
   ├─ branch vlad/feat-xyz <-> route v2026_04_03.patent.propose
   └─ branch bound to route, to drive via hooks
```

#### t2 error case (snapshot target)

```
$ rhx patent.propose

🦅 bummer dude...

⚓ patent.propose
   └─ route already extant: .route/v2026_04_03.patent.propose/

use the extant route or delete it first.
```

#### snapshot coverage plan

- [x] t1 success creation → `.snap`
- [x] t2 already extant error → `.snap`

---

## critical paths

| critical path | description | why critical |
|---------------|-------------|--------------|
| search → fetch → review | user searches, fetches relevant patent, reads claims | core prior art research flow |
| propose → idea → research | user creates route, describes idea, begins research | core proposal flow |
| search with good query | user provides specific query, gets relevant results | if results are poor, tool is useless |
| fetch complete document | user requests patent, gets all claims | claims are the essence of patents |

---

## ergonomics review

| journey | input ergonomics | output ergonomics | friction notes |
|---------|------------------|-------------------|----------------|
| search | natural — query is plain text | natural — relevance scores help prioritize | query too short could yield poor results; add validation |
| fetch | natural — exid from search results | natural — structured output with clear sections | long documents may need pagination or summary mode |
| propose | natural — no required args | natural — tree output shows full route | many stones may feel heavy; add progress indicator |

---

## reproduction feasibility

### test utilities available

| utility | purpose | status |
|---------|---------|--------|
| `spawnSync` | run bash skills from TypeScript | extant |
| `genTempDir` | isolated temp directories per test | extant |
| PATH injection mocks | mock API responses via fake curl | extant |
| snapshot tests | output verification | extant |

### setup required

1. **fixtures**: capture sample API responses for search and fetch
2. **mocks**: PATH injection to avoid live API calls in tests
3. **temp repos**: genTempDir for route creation tests

### concrete test sketch

```typescript
describe('patent.priors.search', () => {
  given('[case1] search with specific query', () => {
    when('[t0] before any changes', () => {
      then('skill file extant', () => {
        expect(fs.existsSync(skillPath)).toBe(true);
      });
    });

    when('[t1] search is run', () => {
      const result = useThen('it returns results', async () => {
        return runSkill(['--query', 'neural network compression']);
      });

      then('exit code is 0', () => {
        expect(result.exitCode).toBe(0);
      });

      then('output matches snapshot', () => {
        expect(result.stdout).toMatchSnapshot();
      });
    });
  });
});
```

---

## gaps

| gap | status | resolution |
|-----|--------|------------|
| live API tests require key | deferred | use fixtures for unit/integration, live for acceptance |
| rate limits unknown | researchable | empirical test, add retry logic |
| editor open verification | deferred | acceptance test with manual verification |

no blockers — all gaps have viable resolutions.

---

## file convention

| file | purpose |
|------|---------|
| `patent.priors.search.play.integration.test.ts` | search journey with mocked API |
| `patent.priors.fetch.play.integration.test.ts` | fetch journey with mocked API |
| `patent.propose.play.integration.test.ts` | route creation journey |
| `patent.flow.play.acceptance.test.ts` | full journey with live API (optional) |

