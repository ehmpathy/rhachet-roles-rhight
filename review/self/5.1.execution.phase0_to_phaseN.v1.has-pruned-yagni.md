# review: has-pruned-yagni

## summary

reviewed for extras that were not prescribed. found one issue and fixed it.

## findings

### issue: expand_context function is dead code

**where:** git.repo.get.operations.sh lines 234-247

**what:** the `expand_context` function was defined but never called anywhere in the codebase. it was scaffolded "for future use" but the actual implementation shows match lines directly without context expansion.

**why it's YAGNI:** adding code "we might need later" violates YAGNI. the function exists but does not do useful work - it just passes stdin through unchanged.

**how fixed:** removed the dead code function. the `--radius` parameter is still accepted and displayed in output (as the criteria requests), but the actual context expansion was never implemented. if context expansion is needed later, it should be implemented when actually needed.

### non-issues

1. **--help flag**: standard shell convention, not extra functionality
2. **tree-style output**: matches extant skills (git.commit, git.rebase) - consistent, not extra
3. **cloud fallback**: explicitly requested in vision
4. **multi-repo search stub**: returns explicit "not yet implemented" error - this is scope limitation message, not dead code
5. **--ref flag**: explicitly requested in criteria for "browse specific versions"
6. **--radius flag**: explicitly requested in criteria - shows in output, context expansion is a separate concern

## verification

all 21 integration tests still pass after removing the dead function.
