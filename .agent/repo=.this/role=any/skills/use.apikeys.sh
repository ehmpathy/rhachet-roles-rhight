#!/usr/bin/env bash
######################################################################
# .what = source API keys for skills that need them
#
# .why  = some skills need API keys (e.g., review, keyrack)
#         this stub exists so guards can source it without error
#
# usage:
#   source .agent/repo=.this/role=any/skills/use.apikeys.sh
#
# note:
#   run `rhx keyrack unlock --owner ehmpath --env test` first
#   to populate the keys
######################################################################

# stub file - keyrack unlock handles the actual key population
# this file exists so guards can source it without "file not found" errors

:  # no-op, returns 0
